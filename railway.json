{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn blog_platform.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120"
  }
}
