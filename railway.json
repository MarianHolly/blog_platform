{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn blog_platform.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
  }
}
