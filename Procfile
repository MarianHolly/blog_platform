web: gunicorn blog_platform.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120
release: python manage.py migrate && python manage.py collectstatic --no-input && python manage.py shell -c "from django.contrib.auth.models import User; from accounts.models import Profile; import os; username = os.getenv('SUPERUSER_USERNAME', 'admin'); email = os.getenv('SUPERUSER_EMAIL', 'admin@blogplatform.com'); password = os.getenv('SUPERUSER_PASSWORD', 'admin123'); User.objects.filter(username=username).exists() or (User.objects.create_superuser(username=username, email=email, password=password, first_name='Platform', last_name='Administrator'), Profile.objects.get_or_create(user=User.objects.get(username=username), defaults={'role': 'admin', 'biography': 'Platform administrator'}))"
